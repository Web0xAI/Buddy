# v3 Rig Build Notes

This package now includes enough structured source material to move from static references into an animated Buddy avatar.

## What is still not final production code

The rig folders are visual layer references, not a finished runtime component.

## What this package enables

- Rive import planning
- React/SVG layer splitting
- mouth/viseme mapping
- eye gaze system
- blink system
- Chucky Mode overlay system
- state machine implementation

## Recommended next deliverable

Create the implementation prompt for Google AI Studio or your app builder.
